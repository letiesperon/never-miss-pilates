{
  "name": "never-miss-pilates",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "playwright install",
    "heroku-postbuild": "playwright install"
  },
  "dependencies": {
    "playwright": "^1.44.1"
  }
}
